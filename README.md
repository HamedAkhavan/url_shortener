# URL Shortener

### Backend Manual Setup

This method will make the development server run on your machine.

**Requirements**

- Python == `3.12`

```bash
$ pip install uv
$ uv sync --all-extras
$ uv run uvicorn main:app --reload
```

### Docs URL

http://localhost:8000/docs
